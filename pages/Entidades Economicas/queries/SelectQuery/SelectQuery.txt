SELECT * FROM public."entidad"
WHERE "entidad" ilike '%{{data_table.searchText || ""}}%'
ORDER BY "{{data_table.sortOrder.column || 'codigo'}}" {{data_table.sortOrder.order || 'ASC'}}
OFFSET {{(data_table.pageNo - 1) * data_table.pageSize}};